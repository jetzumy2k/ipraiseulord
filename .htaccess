<IfModule mod_rewrite.c>
    RewriteEngine On

    # Route all requests through Laravel's public/ folder when the
    # hosting document root points at the project root (common on cPanel).
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
